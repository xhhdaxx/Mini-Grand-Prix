# 项目：Mini Grand Prix · 卡通方程式竞速

## 项目目标

本项目是一个原生 HTML、JavaScript 和 Canvas 2D 实现的六车方程式大奖赛模拟游戏。当前产品方向以真实但易上手的浏览器竞速体验为核心，不再以早期“五车道躲避、导弹清屏、同屏双人”原型作为验收目标。

主流程：

> 主菜单 → 赛道与比赛设置 → 一圈排位赛 → 三圈正赛 → 结算、积分与下一站

也支持单人计时挑战，以及在手机手柄模式下由两台手机参与的双人分屏对决。

## 当前功能范围

1. 驾驶与物理：转向、加速、制动、P 区急停、倒车、碰撞、锁胎、路面限速。
2. 比赛系统：六车发车、AI 竞速、排位、正赛、圈速、实时排名、处罚与黄旗。
3. 赛车策略：ERS 放电和制动回收、轮胎温度与磨损、干地强制换胎规则。
4. 天气系统：晴天、阴天、降雨与可预判的动态天气。
5. 维修区：独立进出站路线、车队 P 房、限速、换胎和安全释放。
6. 内容与存档：二十四条赛道、六支虚构车队、车型调校、排行榜、生涯与二十四站锦标赛。
7. 输入与本地化：纯键盘、局域网手机手柄、双人分屏，以及简体中文和英文界面。

详细验收标准见 `docs/prd.md`。

## 技术栈

- `index.html` 作为页面入口，`src/main.js` 负责游戏循环与流程编排
- `src/` 下使用浏览器原生 ES Module
- Canvas 2D 渲染，不使用游戏引擎
- Node.js 内置 `assert`，由 `tests/run.js` 驱动测试
- 无编译、打包和部署步骤
- 运行时代码不依赖外部前端库

## 代码结构

```text
src/game/
  car.js             车辆控制、物理与碰撞
  ai.js              AI 驾驶
  race-flow.js       排位、正赛、计时与双人流程
  race-systems.js    ERS、天气、轮胎、比赛控制和维修区规则
  track.js           赛道与维修区几何
  track-meta.js      二十四站赛历、位置与本地化元数据
  teams.js           车队配置
  vehicle-config.js  车型预设与性能调校
src/renderer/
  track-render.js    赛道和环境绘制
  car-render.js      赛车绘制
  hud.js             HUD、排名和进站引导
src/utils/
  keyboard.js        键盘状态
  math.js            数学工具
  storage.js         localStorage 存档
src/ui/               菜单与 3D 地球选站
src/gamepad/          手机手柄 WebSocket 客户端
src/i18n.js           中英文界面文案
gamepad.html          手机手柄页面
server.js             静态服务与 WebSocket 通道
tests/run.js          测试入口
```

## 执行规则

1. 修改前读取相关源码、测试以及当前 Git 状态，保护用户已有改动。
2. 规则、物理或赛道几何变更应先补充可失败的测试，再写最小实现。
3. 每次完成变更后优先运行与改动直接相关的快速测试；不要在每一次小修改后重复运行 AI 三圈完赛与强制进站的长时间模拟。
4. `node tests/run.js` 包含 AI 长时间模拟。修改 `ai.js`、车辆物理、比赛策略、维修区规则或赛道几何时，在最终验收阶段运行一次完整测试；纯文档、静态素材或不影响比赛行为的文案修改不要求运行该长测。
5. 涉及 Canvas 布局、菜单或比赛流程时，还应通过本地静态服务器进行浏览器冒烟测试。
6. 提交前进行只读代码审查，运行适用于本次改动的测试以及 `git diff --check`；任何已运行的测试退出码非 `0` 时不得提交。
7. 不自动合并、不部署；只有用户明确要求时才提交或推送。

## 护栏

- 不修改 `.env` 或任何密钥文件。
- 不添加与需求无关的 npm 依赖。
- 不跳过测试或隐藏失败。
- 不执行破坏性 Git 命令，不覆盖用户未提交修改。
- 不把 `localStorage` 数据视为可信输入；读写需要容错。
- 新功能必须与 README、PRD 和游戏内规则说明保持一致。

## 验证闸门

所有改动的基础闸门：

```bash
git diff --check
```

涉及 AI、车辆物理、比赛策略、维修区规则或赛道几何时追加完整闸门（一次即可，无需在每次编辑后重复）：

```bash
node tests/run.js
```

仅修改 README、其他文档或静态素材时，检查引用路径与 Markdown 格式即可，不要求运行 AI 长时间模拟。

验收至少确认：

- 本次改动所适用的测试全部通过；如运行完整测试，则必须全部通过。
- 页面通过本地 HTTP 服务加载且控制台无启动错误。
- 排位赛、正赛和单人计时入口仍可用。
- 没有修改或提交 `.env`、系统文件和临时文件。

## 历史说明

`docs/tdd-log.md` 和 `docs/governance-report.md` 记录了早期五里程碑原型的实施过程，只用于项目复盘。它们不是当前源码结构、测试数量或产品范围的事实来源；当前事实以本文件、`README.md`、`docs/prd.md` 和实际代码为准。
